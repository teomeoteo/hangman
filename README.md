# This is the Hangman Game!

Initially I worked inside sketch.rb

Then I used AI to make a bash script to dynamically create class files with requires

Then I manually made the SavegameManager file

- save.yml is created if "save" is typed in the command line
- if "load" is typed, it will load a game state using save.yml
